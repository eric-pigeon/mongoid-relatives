source 'https://rubygems.org'

# Specify your gem's dependencies in mongoid-relatives.gemspec
gemspec
gem "actionpack",  "~> 4.0.12"
group :test do
  if ENV["CI"]
    gem "coveralls", require: false
  end
end
